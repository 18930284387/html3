diff --git a/index.html b/index.html
index 4fed2db..526bd64 100755
--- a/index.html
+++ b/index.html
@@ -5,13 +5,52 @@
   <meta name="viewport" content="width=device-width,initial-scale=1">
   <title>Smiley Slots</title>
   <link rel="stylesheet" href="style.css">
+  <style>
+    .spin-button-container {
+      display: flex;
+      justify-content: center;
+      margin: 20px 0 10px;
+    }
 
+    .spin-button {
+      appearance: none;
+      border: none;
+      border-radius: 999px;
+      padding: 16px 36px;
+      font-size: 1.1rem;
+      font-weight: 800;
+      letter-spacing: 0.08em;
+      color: #fff;
+      background: linear-gradient(135deg, #ff4d6d 0%, #ff9f1c 100%);
+      box-shadow: 0 14px 30px rgba(255, 77, 109, 0.35), 0 0 0 4px rgba(255, 255, 255, 0.12);
+      cursor: pointer;
+      transition: transform 0.12s ease, box-shadow 0.12s ease, filter 0.12s ease, opacity 0.12s ease;
+    }
+
+    .spin-button:hover:not(:disabled) {
+      transform: translateY(-2px) scale(1.01);
+      box-shadow: 0 18px 34px rgba(255, 77, 109, 0.4), 0 0 0 6px rgba(255, 255, 255, 0.16);
+      filter: brightness(1.05);
+    }
+
+    .spin-button:active:not(:disabled),
+    .spin-button.is-pressed:not(:disabled) {
+      transform: translateY(2px) scale(0.97);
+      box-shadow: 0 8px 18px rgba(255, 77, 109, 0.3), 0 0 0 4px rgba(255, 255, 255, 0.12);
+      filter: brightness(0.94);
+    }
+
+    .spin-button:disabled {
+      opacity: 0.65;
+      cursor: not-allowed;
+      box-shadow: 0 8px 18px rgba(0, 0, 0, 0.22), 0 0 0 4px rgba(255, 255, 255, 0.08);
+    }
+  </style>
 </head>
 <body>
 <!-- Please like <3 and share if you enjoyed! -->
 
-
-<div class="container" onclick="startSpin()">
+<div class="container">
   <h1 class="title"> Smiley Sl😱ts </h1>
   <div class="money-container">
     <div id="money">100</div>
@@ -24,17 +63,23 @@
     <div class="reel-container"></div>
   </div>
 
+  <div class="spin-button-container">
+    <button id="spin-button" class="spin-button" type="button">🎰 SPIN NOW</button>
+  </div>
+
   <div class="info">
     <div class="instructions">
-      Press anywhere to spin the reels.<br/> Each spin costs 1 💰<br/> Match at least two on the middle row to win!<br/> See prizes below.</div>
+      Press the SPIN button to start the reels.<br/> Each spin costs 1 💰<br/> Match at least two on the middle row to win!<br/> See prizes below.
+    </div>
 
     <div class="prize-table">
       <div class="doubles">
       </div>
       <div class="triples">
-
       </div>
     </div>
+  </div>
+
   <script src="script.js"></script>
 </body>
-</html>
\ No newline at end of file
+</html>
diff --git a/script.js b/script.js
index 1686d03..528b0f1 100755
--- a/script.js
+++ b/script.js
@@ -3,6 +3,7 @@
 let reelContents = ["😂", "😍", "😅", "🤔", "😜", "🤐", "😱", "😵"];
 let reelLength = 3;
 let reelContainers = document.querySelectorAll(".reel-container");
+let spinButton = document.querySelector("#spin-button");
 let spinningReels = [];
 let spinning = false;
 let reelDelay = 100;
@@ -28,24 +29,36 @@ let getReelItem = () => {
   return newReel;
 };
 
-let startSpin = () => {
-  if (!spinning && money > 0) {
-    document.querySelectorAll(".prize-item.active").forEach(s => {
-      s.classList.remove("active");
-    });
-    updateMoney(-1);
-    setChange(-1);
-    spinningReels.push(0);
-    setTimeout(() => {
-      spinningReels.push(1);
-    }, reelDelay);
-    setTimeout(() => {
-      spinningReels.push(2);
-    }, reelDelay * 2);
+let updateSpinButtonState = () => {
+  if (!spinButton) return;
+  spinButton.disabled = spinning || money <= 0;
+  if (spinning) spinButton.innerText = "🎰 SPINNING...";
+  else if (money <= 0) spinButton.innerText = "💸 OUT OF COINS";
+  else spinButton.innerText = "🎰 SPIN NOW";
+};
 
-    spinning = true;
-    spinUpdate(7);
+let startSpin = () => {
+  if (spinning || money <= 0) {
+    updateSpinButtonState();
+    return;
   }
+
+  document.querySelectorAll(".prize-item.active").forEach(s => {
+    s.classList.remove("active");
+  });
+  updateMoney(-1);
+  setChange(-1);
+  spinningReels.push(0);
+  setTimeout(() => {
+    spinningReels.push(1);
+  }, reelDelay);
+  setTimeout(() => {
+    spinningReels.push(2);
+  }, reelDelay * 2);
+
+  spinning = true;
+  updateSpinButtonState();
+  spinUpdate(7);
 };
 
 let spinUpdate = spinsLeft => {
@@ -67,6 +80,7 @@ let spinUpdate = spinsLeft => {
     } else {
       spinning = false;
       findWins();
+      updateSpinButtonState();
     }
   }
 };
@@ -85,6 +99,7 @@ let moveReel = reelIndex => {
 let updateMoney = change => {
   money += change;
   document.querySelector("#money").innerText = money;
+  updateSpinButtonState();
 };
 
 let setChange = change => {
@@ -207,4 +222,25 @@ reelContents.forEach((symbol, index) => {
     (index + 1) * 100,
     "triples"
   );
-});
\ No newline at end of file
+});
+
+if (spinButton) {
+  ["pointerdown", "keydown"].forEach(eventName => {
+    spinButton.addEventListener(eventName, () => {
+      spinButton.classList.add("is-pressed");
+    });
+  });
+
+  ["pointerup", "pointerleave", "keyup", "blur"].forEach(eventName => {
+    spinButton.addEventListener(eventName, () => {
+      spinButton.classList.remove("is-pressed");
+    });
+  });
+
+  spinButton.addEventListener("click", () => {
+    if (audioCtx.state === "suspended") audioCtx.resume();
+    startSpin();
+  });
+}
+
+updateSpinButtonState();
